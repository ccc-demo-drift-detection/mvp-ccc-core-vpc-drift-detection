# Scripts

Helper scripts used by checks and Make targets.
- `lib/`: shared env loader and helpers.
- `snapshot.sh`, `history-diff.sh`: snapshot/diff utilities for outputs.
- Other scripts support reporting, cleanup, and drift utilities.
