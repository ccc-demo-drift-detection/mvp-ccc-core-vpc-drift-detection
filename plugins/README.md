# Plugins

Holds the Privateer plugin submodule.
- `plugin-ccc-vpc`: Privateer-based CCC VPC plugin (source + docs under `docs/`, catalog/config under `ccc-yaml/`).
- After checkout, run `git submodule update --init --recursive` to populate.
